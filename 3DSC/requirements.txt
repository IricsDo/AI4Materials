ase
gemmi
joblib
numpy>=1.26.4,<3.0          # Modern NumPy (fully supports Python 3.12 + NumPy 2.x)
pandas>=2.0                 # pandas 2+ is fine now
pymatgen>=2024.0            # Much newer versions (2024–2026) are compatible with Python 3.12 and NumPy 2.x
ruamel.yaml                 # No upper pin needed (modern versions work fine)
tqdm

# Optional dependencies (uncomment if you need them)
# dscribe                     # Updated name; no longer needs old pin
# chemml
# megnet
# tensorflow                  # or tensorflow-cpu if you don't need GPU